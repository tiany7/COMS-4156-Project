syntax = "proto3";
message echoRequest {
    string message = 1;
}

message echoResponse {
    string message = 1;
}

service echoService {
    rpc echo(echoRequest) returns (echoResponse);
}